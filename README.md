# Lombok, a syntactic sugar library for Java

Notes:
* Lombok is compiler dependent
* You're IDE requires a Lombok plugin
* [JEP-286 : Type Inference](http://openjdk.java.net/jeps/286) for `val` / `var`
